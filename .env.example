# PostgreSQL -- required
DB_HOST=localhost
DB_PORT=5432
DB_NAME=drone_locations
DB_USER=drone_app
DB_PASSWORD=replace_with_your_password

# Optional: without this key, the app still works but does not show wind speed.
OPENWEATHER_API_KEY=

# Reserved for the optional embedded Google Street View feature.
GOOGLE_MAPS_EMBED_API_KEY=
